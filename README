This is a collection of reference applications that will run on any WSGI server (mod_wsgi, Gunicorn, uWSGI, etc).

Its purpose is to serve JSON on RESTful routes. It is meant to serve to Javascript browser clients, but could be used for any JSON producer or consumer.
